program {

    var int x;
    var int y;
    var int w;
    let int z;

    x = 10;
    y = 20;
    z = x + y * 2;

    if (z > 30) {
        w = z / 2;
    } else {
        w = z - 5;
    }

    print(w);

}